Current version: 1.2.0
    NEW support for memcached options, currently limited to memory size
    NEW support for memcached options, currently limited to memory size

tag: 1.1.2
    NEW memcache:get_pools/0
    FIX Environment preconfiguration fixes
    NEW memcache:get_pools/0
    FIX properly register memcached instances
    FIX children startup order

tag: 1.1.1
    NEW set memcache pools via environment config
    NEW set memcache pools via environment config

tag: 1.1.0
    NEW memcache:stats/1

tag: 1.0.10
    NEW erlanglibs 2.7.3

tag: 1.0.9
    OPT Improved a testcase with a retry mechanism
    OPT memcached availability verification

tag: 1.0.8
    OPT timeout increased for one test
    FIX some extra time given to memcached to die after kill -9

tag: 1.0.7
    CHANGE shared pools functionality rolled back
    NEW Allowing pools sharing address
    FIX added error handling for race condition in closing sock

tag: 1.0.6

tag: 1.0.5
    FIX Another attempt to improve behaviour on CI
    FIX Attempt to fix socket reuse issue on jenkins
    OPT Added unit test for single pool restart

tag: 1.0.4
    FIX graceful poolboy shutdown

tag: 1.0.3
    DOC added overview.edoc
    OPT poolboy dependency updated

tag: 1.0.2
    FIX poolboy version verified in rebar.config

tag: 1.0.1
    FIX supervisor report when stopping a pool

tag: 1.0.0
    OPT Added forgotten test module
    OPT Error handling improved when a pool dies
    CHANGE emptied just before updating the newest version
    NEW First fully functional version
    FIX dialyzer passing
    NEW Initial version

tag: 0.1.0
    FIX configs
    NEW more tests
    NEW added tests
    FIX erlanglibs version
    CHANGE Added config and runner scripts.
    FIX dependencies poolboy and erlanglibs
